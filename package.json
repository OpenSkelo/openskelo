{
  "name": "openskelo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @openskelo/gates && npm run build -w @openskelo/adapters && npm run build -w @openskelo/queue",
    "test": "vitest run",
    "dev": "vitest watch"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
